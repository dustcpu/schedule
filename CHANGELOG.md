# 更新日志

本文件记录**每一次推送到 GitHub 的变更**。

## 维护约定

1. **推送前先更新本文件** —— 把本次改动写进 `[未发布]` 区，再提交推送。
2. **发布新版本时**：把 `[未发布]` 的内容转成新的版本小节（带日期），并打 tag：
   ```bash
   git tag v0.1.4 && git push origin v0.1.4
   ```
3. 分类固定用：`新增` / `修复` / `变更` / `文档` / `移除`。
4. 写"用户能感知到什么"，而不是罗列 commit。技术细节留给 commit message。

## 版本号所在位置（发版时同步修改）

| 位置 | 当前值 | 说明 |
|---|---|---|
| `src-tauri/tauri.conf.json` | `0.1.4` | 安装包版本（外壳） |
| `src-tauri/Cargo.toml` | `0.1.4` | Rust 包版本 |
| `src-tauri/Cargo.lock` | `0.1.4` | 锁文件里本包版本（⚠️ 发版时易漏，需与 Cargo.toml 同步） |
| `README.md` | `v0.1.4` | 文档头部 |
| `engine/scheduler/__init__.py` | **`0.1.0`** | 引擎独立版本号 ⚠️ 与外壳不同步，待统一 |

---

## [未发布]

### 变更
- **连堂位置收窄为只能在【1-2 节】或【4-5 节】**：原先允许的起始节次是 `[1,2,3,4]`
  （即 1-2 / 2-3 / 3-4 / 4-5），现改为 `[1,4]`，不再允许 2-3、3-4 等中间位置。

  依据：一位老师通常带 2 个班，一个班排上午 1-2 节、另一个班排上午 4-5 节，
  两个班错开便于教师连堂授课——这正是 `hard_limits` 里"连堂规则"一栏描述的原始意图。

  实测（25 班 × 5 方案 × 3 科 = 375 组）：起始于第 1 节 180 组、第 4 节 195 组、
  **其他位置 0 组**；求解耗时 10.8 秒，`verify_hard.py` H1–H5 全部通过。

  > 该参数目前写在内核默认值里（`scheduler/config.py` 的 `ConsecutiveRule.allow_start_periods`），
  > 外壳设置页尚未开放配置。

---

## [v0.1.4] - 2026-09-26

> **本版修复连堂（走班/连堂规则）的两处实现缺陷。**

### 新增
- **测试存档目录 `测试存档/`**：记录每次测试的结果与分析，含 `README.md` 索引与命名约定。
  首份存档为《2026-09-25 引擎问题分析》。
- **参考资料目录 `参考资料/`**：收录 13 篇排课算法论文 PDF（均为开放获取渠道），
  以及付费墙文献清单。
- **上传文件校验**：选择输入文件时先校验结构，不合法直接提示。
- **检查更新**功能。

### 修复
- **连堂学科在非指定日也会长出第二个连堂**：H5b 的相邻排课禁则把 `blen > 1` 的学科整个跳过了，
  导致语数英在指定日之外的四天不受约束，求解器会在别的天再排一个双节连堂。
  实测 25 班 × 5 方案共 146 处违规。
- **连堂学科课时分布不均**：只禁相邻后，语数英余下的课时会挤在少数几天
  （实测 319 处"某天两节、另一天零节"）。
  现改为「非指定日每天至多 cap 节」，`cap = ceil((周课时 - 连堂块) / 非指定日数)`，
  使课表呈"每天一节 + 指定日连堂"。

  修复后（25 班 × 5 方案）：
  | 指标 | 修复前 | 修复后 |
  |---|---|---|
  | 指定日恰好一次连堂 | 375 | **375** |
  | 非指定日相邻双节 | 146 | **0** |
  | 非指定日课时分布不均 | 319 | **0** |
  | 求解耗时 | 11.5s | 14.2s |

  > `cap` 按周课时自动推算而非固定为 1，是为了保证**永远可行**：
  > 语数英 7 节/周时 `cap = 2`，此时会在 `status.json` 的 `warnings` 中
  > 如实告知"已放宽为每天至多 2 节"，不会直接排不出课表。

### 变更
- 测试存档不再保留 PDF，只保留课表 `xlsx`（PDF 体积大、内容与 xlsx 重复）。

---

## [v0.1.3] - 2026-09-24

> tag: `v0.1.3`　commit: `719fae7`

**本版是算法内核落地版。**

### 新增
- **CP-SAT 排课内核**：真正的排课引擎替代原占位实现 `mock_engine.py`。
  - 硬约束 H1–H5：每格唯一、周课时守恒、教师不冲堂、固定课、连堂
  - 软约束 S1–S5：主科分散、体育不排首末、教师日均均衡、首节主科、自习后置（可配权重）
  - 多解输出：3–8 套方案
  - 分层结构：`engine.py` 入口 + `scheduler/`（config / data / solver / export）
- **`input.xlsx` 模板及字段说明**（`engine/input_template.md`）：
  定义「班级 / 教师 / 课程」三个 sheet 的数据契约，及硬性校验规则。
- **硬约束自检器** `engine/verify_hard.py`：独立复核 H1–H5（跑通 ≠ 正确）。
- 测试数据 `engine/test_input/`（4 班）与 `engine/test_input_25/`（25 班）。
- 输入模板文件放到 `src-tauri/resources/`。

### 修复
- **非语数英伪连堂**：非连堂学科被排成相邻两节，违反连堂语义。

### 变更
- 仓库结构整理：`安装说明.md`、`接口协议.md`、`排课约束目录.md` 移入 `docs/`。

---

## [v0.1.2] - 2026-09-22

> tag: `v0.1.2`　commit: `35e7a1a`

### 新增
- **新手引导教程**：首次打开自动弹出 6 步教学，设置页可重新查看。
- **年级切换**：主界面支持高一 / 高二 / 高三，各年级的特殊要求独立保存、互不丢失。

### 修复
- 新手教程的设置入口。

---

## [v0.1.1] - 2026-09-22

> tag: `v0.1.1`　commit: `c4abcae`

### 新增
- 用户体验优化：进度动画、错误提示友好化、结果卡片、窗口位置记忆、
  硬限制摘要、文件校验、临时目录清理。

### 修复
- **引擎超时**：外壳调用引擎时未设超时，引擎卡死会连带拖死界面（接口协议 §2 要求 10 分钟强杀）。
- **午休缺失**：节次时间计算从上午开始线性累加，忽略午休边界，
  导致下午节次整体前移约 2 小时。

---

## [v0.1.0] - 2026-09-20

> tag: `v0.1.0`　commit: `4e211b5`

### 新增
- 仓库初始化：Tauri 2 外壳（Rust + 原生 HTML/CSS/JS）+ Python 引擎的桌面排课工具框架。
- **硬限制预设条件**：时间安排、固定课程、班级设置、连堂规则（设置页可维护）。
- 托盘常驻、配置存储、调用引擎、结果展示等外壳能力。
- 开发占位引擎 `mock_engine.py`（多解输出骨架，供 UI 联调）。
