# 测试存档

本目录用于**记录历史测试结果与问题分析**。

- **只记录测试与结论，不参与软件构建** —— 引擎、外壳、UI 的运行都不读取本目录。
- 命名约定：`YYYY-MM-DD-测试主题/`
- 每次测试建议包含：`分析报告.md`（现象/根因/方案）+ 原始输出 + 复现脚本。

---

## 存档索引

| 日期 | 主题 | 被测版本 | 结论摘要 |
|---|---|---|---|
| 2026-09-25 | [引擎问题分析](./2026-09-25-引擎问题分析/分析报告.md) | `a0b072d` | 引擎跑通、性能充足（25 班 11.5 秒）；发现 3 个问题：多解质量不稳定、评分命名无区分度、无解诊断缺失 |

---

## 目录结构

```
测试存档/
└── 2026-09-25-引擎问题分析/
    ├── 分析报告.md           完整分析：现象 → 根因（带代码行号）→ 影响 → 方案
    ├── 25班-引擎实测/        25 个班的引擎输出（问题证据）
    │   ├── result.xlsx       总览 + 方案1~5，每方案含全部 25 个班的完整课表
    │   └── status.json       5 套方案评分/描述（全为 100.0、描述雷同）
    └── 压测脚本/             独立 CP-SAT 压测模型（不依赖本项目引擎）
        ├── cpsat_bench.py
        ├── cpsat_bench2.py
        └── cpsat_bench3.py
```

---

## 两条阅读提醒

**1. 区分「引擎实测」与「独立压测」。**

- `25班-引擎实测/` = **本项目引擎**跑 `engine/test_input_25/` 的真实结果，分析报告第 3、4 节的数据全部来自这里。
- `压测脚本/` = **另写的独立模型**（不 import 项目代码、数据自建），只用于验证修复方法的可行性。
  它给出的「差异化权重可达 86% 差异度」**不等于**项目引擎改完后的效果。

**2. 存档只保留课表 xlsx，不保留 PDF。**

仓库 `.gitignore` 忽略了 `*.pdf`，测试存档**不放 PDF**（约定：PDF 体积大、内容与 xlsx 重复）。

- **要保留**：`result.xlsx` —— 完整课表。每个方案 sheet 含全部班级的课表，
  已验证 25 班 × 8 节 × 5 天 = 1000 格全部填满，无空缺。
- **不保留**：`result.pdf` —— 需要时按下方命令重新生成即可。

`.gitignore` 为本目录开了 xlsx 例外：`!测试存档/**/*.xlsx`。

---

## 复现方式

```bash
cd engine
uv run --with ortools --with openpyxl --with reportlab \
    python engine.py test_input_25 <输出目录> <任务ID>
```
